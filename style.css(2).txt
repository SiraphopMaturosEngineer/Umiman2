/* ==========================================================================
   StudyMate Essentials - Minimalist & Modern Student Theme
   Colors: Peach Orange & Pastel Blue
   ========================================================================== */

@import url('https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;600;700&display=swap');

/* CSS Variables */
:root {
  /* Color Palette */
  --primary-peach: #FF8A7A;
  --primary-peach-hover: #F07362;
  --primary-peach-light: #FFF0ED;
  
  --secondary-blue: #70A6FF;
  --secondary-blue-hover: #528EFF;
  --secondary-blue-light: #EEF4FF;
  
  --accent-soft-yellow: #FFF3D6;
  --bg-main: #FAFCFF;
  --bg-card: #FFFFFF;
  
  --text-dark: #2C3E50;
  --text-muted: #7F8C8D;
  --text-light: #A0AEC0;
  
  --border-color: #E2E8F0;
  --border-radius-sm: 8px;
  --border-radius-md: 16px;
  --border-radius-lg: 24px;
  --border-radius-pill: 50px;
  
  --shadow-sm: 0 2px 8px rgba(112, 166, 255, 0.08);
  --shadow-md: 0 8px 20px rgba(255, 138, 122, 0.12);
  --shadow-lg: 0 15px 30px rgba(44, 62, 80, 0.1);
  
  --transition-fast: all 0.2s ease;
  --transition-normal: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
  
  --container-max-width: 1200px;
}

/* Reset & Base Styles */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
  font-size: 16px;
}

body {
  font-family: 'Prompt', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  background-color: var(--bg-main);
  color: var(--text-dark);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

img {
  max-width: 100%;
  height: auto;
  display: block;
}

a {
  text-decoration: none;
  color: inherit;
  transition: var(--transition-fast);
}

button {
  cursor: pointer;
  border: none;
  outline: none;
  font-family: inherit;
  transition: var(--transition-normal);
}

/* Container */
.container {
  width: 90%;
  max-width: var(--container-max-width);
  margin: 0 auto;
  padding: 0 15px;
}

/* Header & Navbar */
.navbar {
  position: sticky;
  top: 0;
  z-index: 1000;
  background-color: rgba(255, 255, 255, 0.92);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--border-color);
  padding: 1rem 0;
  box-shadow: var(--shadow-sm);
}

.navbar .container {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.logo {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-dark);
}

.logo span {
  color: var(--primary-peach);
}

.logo-icon {
  width: 38px;
  height: 38px;
  background: linear-gradient(135deg, var(--primary-peach), var(--secondary-blue));
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 1.2rem;
}

.nav-links {
  display: flex;
  list-style: none;
  gap: 2rem;
  align-items: center;
}

.nav-links a {
  font-weight: 500;
  color: var(--text-dark);
}

.nav-links a:hover {
  color: var(--primary-peach);
}

.cart-btn {
  position: relative;
  background: var(--secondary-blue-light);
  color: var(--secondary-blue);
  padding: 0.6rem 1.2rem;
  border-radius: var(--border-radius-pill);
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8px;
}

.cart-btn:hover {
  background: var(--secondary-blue);
  color: white;
  transform: translateY(-2px);
}

.cart-badge {
  background: var(--primary-peach);
  color: white;
  font-size: 0.75rem;
  padding: 2px 7px;
  border-radius: 10px;
  font-weight: 700;
}

/* Hero Section */
.hero {
  padding: 4rem 0 3rem;
  background: linear-gradient(135deg, var(--primary-peach-light) 0%, var(--secondary-blue-light) 100%);
  border-radius: 0 0 var(--border-radius-lg) var(--border-radius-lg);
  margin-bottom: 3rem;
  text-align: center;
}

.hero-content {
  max-width: 750px;
  margin: 0 auto;
}

.hero-badge {
  display: inline-block;
  background: white;
  color: var(--primary-peach);
  padding: 6px 18px;
  border-radius: var(--border-radius-pill);
  font-size: 0.9rem;
  font-weight: 600;
  box-shadow: var(--shadow-sm);
  margin-bottom: 1.2rem;
}

.hero h1 {
  font-size: 2.8rem;
  font-weight: 700;
  line-height: 1.25;
  color: var(--text-dark);
  margin-bottom: 1rem;
}

.hero h1 span {
  background: linear-gradient(45deg, var(--primary-peach), var(--secondary-blue));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.hero p {
  font-size: 1.15rem;
  color: var(--text-muted);
  margin-bottom: 2rem;
}

/* Filter & Search Bar */
.filter-section {
  margin-bottom: 2.5rem;
}

.category-wrapper {
  display: flex;
  gap: 10px;
  overflow-x: auto;
  padding: 10px 5px;
  justify-content: center;
  scroll-behavior: smooth;
  -webkit-overflow-scrolling: touch;
}

.category-wrapper::-webkit-scrollbar {
  display: none;
}

.cat-btn {
  background: white;
  color: var(--text-muted);
  border: 1px solid var(--border-color);
  padding: 0.6rem 1.4rem;
  border-radius: var(--border-radius-pill);
  font-size: 0.95rem;
  font-weight: 500;
  white-space: nowrap;
  box-shadow: var(--shadow-sm);
}

.cat-btn:hover {
  border-color: var(--primary-peach);
  color: var(--primary-peach);
  transform: translateY(-2px);
}

.cat-btn.active {
  background: var(--primary-peach);
  color: white;
  border-color: var(--primary-peach);
  box-shadow: var(--shadow-md);
}

/* Product Section */
.section-title {
  font-size: 1.8rem;
  font-weight: 700;
  margin-bottom: 1.5rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.product-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  gap: 1.8rem;
  margin-bottom: 4rem;
}

/* Product Card */
.product-card {
  background: var(--bg-card);
  border-radius: var(--border-radius-md);
  overflow: hidden;
  border: 1px solid var(--border-color);
  box-shadow: var(--shadow-sm);
  transition: var(--transition-normal);
  display: flex;
  flex-direction: column;
  position: relative;
}

.product-card:hover {
  transform: translateY(-8px);
  box-shadow: var(--shadow-md);
  border-color: var(--primary-peach-light);
}

.product-img-wrapper {
  width: 100%;
  height: 200px;
  background-color: #F8FAFC;
  position: relative;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}

.product-img-wrapper img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: var(--transition-normal);
}

.product-card:hover .product-img-wrapper img {
  transform: scale(1.06);
}

.category-tag {
  position: absolute;
  top: 12px;
  left: 12px;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(4px);
  color: var(--text-dark);
  font-size: 0.75rem;
  font-weight: 600;
  padding: 4px 10px;
  border-radius: var(--border-radius-pill);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.product-content {
  padding: 1.2rem;
  display: flex;
  flex-direction: column;
  flex-grow: 1;
}

.product-title {
  font-size: 1.1rem;
  font-weight: 600;
  color: var(--text-dark);
  margin-bottom: 0.5rem;
  line-height: 1.4;
}

.product-desc {
  font-size: 0.88rem;
  color: var(--text-muted);
  margin-bottom: 1.2rem;
  flex-grow: 1;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.product-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: auto;
  padding-top: 0.8rem;
  border-top: 1px dashed var(--border-color);
}

.product-price {
  font-size: 1.3rem;
  font-weight: 700;
  color: var(--primary-peach);
}

.product-price::before {
  content: "฿";
  font-size: 0.9rem;
  margin-right: 2px;
}

.add-cart-btn {
  background: var(--secondary-blue-light);
  color: var(--secondary-blue);
  width: 40px;
  height: 40px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.1rem;
  transition: var(--transition-fast);
}

.product-card:hover .add-cart-btn {
  background: var(--primary-peach);
  color: white;
}

.add-cart-btn:hover {
  transform: scale(1.1);
}

/* Footer */
footer {
  background: #1E293B;
  color: #94A3B8;
  padding: 3rem 0 1.5rem;
  margin-top: auto;
}

.footer-content {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 2rem;
  margin-bottom: 2rem;
}

.footer-col h3 {
  color: white;
  font-size: 1.2rem;
  margin-bottom: 1rem;
  font-weight: 600;
}

.footer-col p, .footer-col li {
  font-size: 0.9rem;
  margin-bottom: 0.6rem;
}

.footer-bottom {
  text-align: center;
  padding-top: 1.5rem;
  border-top: 1px solid #334155;
  font-size: 0.85rem;
}

/* ==========================================================================
   Responsive Styles
   ========================================================================== */

/* Tablet (max-width: 768px) */
@media (max-width: 768px) {
  .hero h1 {
    font-size: 2.1rem;
  }

  .hero p {
    font-size: 1rem;
  }

  .category-wrapper {
    justify-content: flex-start;
  }

  .product-grid {
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 1.2rem;
  }
  
  .nav-links {
    display: none; /* Can be toggled with JS hamburger menu */
  }
}

/* Mobile (max-width: 480px) */
@media (max-width: 480px) {
  .hero {
    padding: 2.5rem 0 2rem;
    border-radius: 0 0 var(--border-radius-md) var(--border-radius-md);
  }

  .hero h1 {
    font-size: 1.7rem;
  }

  .product-grid {
    grid-template-columns: 1fr;
  }

  .product-img-wrapper {
    height: 180px;
  }
  
  .logo {
    font-size: 1.2rem;
  }
}
